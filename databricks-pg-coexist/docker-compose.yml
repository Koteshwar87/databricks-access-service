services:
  pg:
    image: postgres:16-alpine
    container_name: holdings-pg
    environment:
      POSTGRES_DB: holdings
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - ./init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d holdings"]
      interval: 5s
      timeout: 3s
      retries: 5
